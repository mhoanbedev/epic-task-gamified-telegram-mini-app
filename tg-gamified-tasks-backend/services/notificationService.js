console.log('Loading notificationService (placeholder)...');

const sendTelegramMessage = async (telegramUserId, message) => {
  console.log(`[NotificationService - Placeholder] Sending to ${telegramUserId}: ${message}`);
  
};

module.exports = {
  sendTelegramMessage,
 
};